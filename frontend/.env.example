REACT_APP_API_URL=http://localhost:3001

# OpenTelemetry: OTLP endpoint for browser traces. Unset = same-origin /api/otel (proxied via server.js to backend).
# Azure Web App: leave unset when using server.js; or set to backend URL + /api/otel
# REACT_APP_OTEL_EXPORTER_OTLP_ENDPOINT=
# REACT_APP_OTEL_SERVICE_NAME=tsp-frontend